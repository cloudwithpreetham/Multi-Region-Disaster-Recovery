# NOTES
AGENT.md
*.zip

# Terraform
.terraform/
# .terraform.lock.hcl is intentionally tracked — pins provider versions for reproducible applies across machines/CI
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# local-only var files (never the tracked terraform/environments/*.tfvars)
terraform.tfvars
*.auto.tfvars
